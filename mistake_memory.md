# RecordReader mistake memory

这份文档记录本项目已经踩过的坑、根因和以后开发时必须提前检查的点。目标不是复盘情绪，而是减少同类 bug 重复出现。

## 1. 导入音频不能覆盖已有播放列表

### 已发生的问题

用户先添加音频 A，再添加音频 B 后，A 从列表中消失，上一首/下一首也无法在 A 和 B 之间切换。

### 根因

导入入口把 `selectedSources` 和 `recordings` 直接替换成“本次选择”的扫描结果。这个逻辑适合“切换文件夹”，但不适合“逐个添加音频到播放列表”。

### 以后必须遵守

- “添加音频”是追加语义，不是替换语义。
- 除非用户明确删除记录或切换到新的独占来源，否则已添加且未删除的录音必须保留在列表中。
- 上一首/下一首必须基于当前可见播放列表，而不是本次导入的临时结果。
- 导入逻辑要明确区分：
  - 选择文件夹：可视为一个来源集合。
  - 选择单个/多个音频：追加到当前来源集合。
  - 删除记录：只隐藏播放记录，不删除源文件。

### 开发检查

- 连续添加两个不同音频，列表应有 2 条。
- 添加第二个音频后，当前播放项应切到新音频。
- 点击上一首应回到前一个音频。
- 删除其中一个记录后，刷新不应把它重新带回来。

## 2. 文件选择和安全作用域要按 iOS 真机行为设计

### 已发生的问题

文件选择后点击 Open 没反应、选择文件夹无响应、重新打开失败后出现“无法读取音频时长”等问题。

### 根因

模拟器/桌面文件路径和 iOS 文件选择器返回的 security-scoped URL 行为不一致。直接用 POSIX 可读性、假设文件路径永久可访问，容易在真机或自签包里失败。

### 以后必须遵守

- iOS 文件 picker 返回的 URL 要先 `startAccessingSecurityScopedResource()`。
- 直接选择音频时，优先 copy 到 app sandbox，减少权限生命周期问题。
- 文件夹必须 open-in-place，不能假设 copy 行为。
- 错误状态不能污染下一次选择同一文件的加载流程。
- `FileManager.isReadableFile` 只能作为诊断日志，不应作为最终可用性判断。

### 开发检查

- 真机选择 MP3 后能立刻播放。
- 真机选择 M4A/WAV/CAF 后能显示时长。
- 失败后重新选择同一文件，不能卡在旧错误状态。
- 选择文件夹和选择单文件都要分别测试。

## 3. App Icon 必须验证“源码存在”和“IPA 打包存在”

### 已发生的问题

源码里有 `AppIcon.appiconset`，但 IPA 里没有 `Assets.car`，实际没有打包进 app。

### 根因

只创建了 asset catalog 文件，但 XcodeGen target 没有正确声明资源目录；另外 AppIcon 只有 1024 图，不是完整 iPhone icon 集。

### 以后必须遵守

- 图标不是“文件存在”就算完成，必须确认进入编译产物。
- XcodeGen 需要把 `Assets.xcassets` 所在资源目录挂到 target。
- `ASSETCATALOG_COMPILER_APPICON_NAME` 必须指向 `AppIcon`。
- IPA 检查必须看到 `Assets.car` 或 app icon 相关输出。

### 开发检查

- `AppIcon.appiconset/Contents.json` 包含 iPhone 常用尺寸和 `ios-marketing`。
- GitHub Actions 构建后下载最新 IPA 到 `.build/github-artifacts`。
- `unzip -l RecordReader-unsigned.ipa` 能看到 `Assets.car`。
- `latest-unsigned-ipa` tag 必须对齐当前 commit，不能误下载旧包。

## 4. 字幕识别要从音频文件本身识别，不是录设备输出

### 已发生的问题

需求曾被误解成监听设备声音；用户明确要求即使戴耳机也要从音频文件本身生成字幕。

### 根因

把“播放器字幕”理解成实时麦克风/系统输出监听，而不是文件级 ASR。

### 以后必须遵守

- 字幕识别输入必须是用户选择的音频文件 URL。
- 识别流程要可在后台读取文件、分窗、生成时间轴字幕。
- 不能依赖设备外放声音，也不能依赖麦克风采集。
- UI 文案要写“为这段录音生成中文字幕”，不要暗示实时听音。

### 开发检查

- 戴耳机播放不影响识别。
- 不播放音频也可以识别字幕。
- 识别结果应写回该录音的 metadata，而不是仅存在临时 UI 状态。

## 5. SFSpeech 只能兜底，长音频主路径必须本地模型

### 已发生的问题

大于 10 分钟、20MB 以上音频使用 SFSpeech 经常超时或只返回部分字幕。

### 根因

Apple Speech 更适合短音频和系统服务兜底，不适合本项目“长录音本地识别”的主路径。

### 以后必须遵守

- 主路径使用 sherpa-onnx Paraformer 中文 int8。
- Apple Speech 只作为失败兜底。
- 长音频必须分窗处理，并记录窗口级进度和耗时。
- UI 必须显示用户能理解的进度，不显示“窗口 1/27”这种内部术语作为主文案。

### 开发检查

- 10 分钟以上 MP3 能完整跑完。
- 每个窗口失败/空字幕要有日志。
- 用户看到的是“正在识别第 x 分钟附近”或百分比进度。
- debug 模式再显示线程数、窗口长度、窗口耗时。

## 6. CoreML / WhisperKit 实验不能只看速度

### 已发生的问题

CoreML 后端极快但 0 字幕；WhisperKit small/base 可以跑，但中文准确率低、出现重复“小声点”等幻觉。

### 根因

推理耗时、模型包大小和识别准确率没有同时作为验收标准。CoreML provider 或模型格式不兼容时，速度快反而可能代表模型没有有效推理。

### 以后必须遵守

- ASR 实验必须同时验证：速度、准确率、时间轴、空字幕率、包体积。
- 任何“0 字幕但速度极快”的结果都应视为失败，而不是优化成功。
- Whisper/CoreML 路线必须用固定中文样本做准确率对比，再考虑集成。
- sherpa 当前 CPU 路线是稳定基线，实验路线不能影响主线可用性。

### 开发检查

- 同一段 11 分钟中文音频，比较字幕内容而不只看耗时。
- 记录重复句、幻觉句、漏识别比例。
- 实验分支隔离大模型和风险依赖，不污染 main。

## 7. 性能优化不能假设“并行一定更快”

### 已发生的问题

CPU 并行后识别时间从约 40 秒变成 50 多秒，出现负优化。

### 根因

Paraformer 推理本身已吃满关键资源，并行窗口带来模型实例、内存带宽、调度和解码开销。iPhone 高端芯片也不是无限并行收益。

### 以后必须遵守

- 性能优化必须以真机日志为准，不能靠 CPU 核心数推断。
- 并行、线程数、窗口长度都要可配置、可回退。
- 默认配置要保守，debug 才暴露 Auto/1/2/4/6/8 等选项。
- 先做诊断日志，再谈优化。

### 开发检查

- 每窗口耗时、平均耗时、最慢窗口、空窗口数都要记录。
- 比较线程数时使用同一音频、同一窗口长度。
- 若并行负优化，默认回退串行或低并发。

## 8. UI 功能入口不能重复

### 已发生的问题

主界面底部快捷栏放了全部、收藏、分类等入口，和录音库/更多菜单功能重复，也不像真正的底部 tab。

### 根因

把“快捷按钮”误当成“主导航”。播放器主屏承载了太多录音库管理功能，导致界面啰嗦。

### 以后必须遵守

- 主屏只服务：导入/选择录音、播放控制、字幕查看。
- 搜索、排序、筛选、批量管理放在录音库。
- 设置放右上角，不做底部 tab。
- 字幕识别入口放字幕区域，不能同时放更多菜单和底部栏。
- 已有功能不要重复出现，只移动到更合理的位置。

### 开发检查

- 主屏顶部导航不超过 3 个主要入口。
- 没有录音时，导入入口只在空状态主按钮出现。
- 有录音时，继续导入合并为一个 `+` 菜单。
- 调试日志不直接占主界面。

## 9. 打包验证必须防止下载旧 IPA

### 已发生的问题

下载 release IPA 时曾拿到旧包，误以为当前提交已打包。

### 根因

只看下载成功，不看 `latest-unsigned-ipa` tag 是否已经更新到当前 commit。

### 以后必须遵守

- 每次打包后先比较：
  - `git rev-parse HEAD`
  - `git ls-remote origin refs/tags/latest-unsigned-ipa`
- tag 对齐当前 commit 后才能下载 IPA。
- 下载后检查包内时间戳和 `Assets.car`。
- 本地 `.build/github-artifacts/RecordReader-unsigned.ipa` 要覆盖成最新产物。

### 开发检查

- tag hash == current commit hash。
- IPA 包内时间戳晚于当前构建时间。
- IPA 大小合理，不能出现异常 230KB 小包。

## 10. 文案要面向用户，不暴露内部实现

### 已发生的问题

识别进度曾显示“窗口数/27”这类内部概念，用户无法理解。

### 根因

直接把工程调试指标展示给用户，没有区分用户态文案和 debug 文案。

### 以后必须遵守

- 用户态文案说结果、进度和下一步动作。
- debug 文案说线程、窗口、耗时和后端。
- 失败提示必须可行动：文件不可读、人声不清晰、模型不可用、可重试。

### 开发检查

- 普通用户不需要理解“窗口、线程、provider”也能知道当前状态。
- debug 日志里保留足够工程诊断信息。
- 失败态必须有重试入口或明确下一步。

## 11. 最小回归清单

每次改导入、播放、字幕、列表、打包相关代码后，至少跑下面的人工/自动检查：

1. 选择单个 MP3，能加入列表并播放。
2. 再选择第二个 MP3，列表保留两个，当前切到第二个。
3. 上一首/下一首可在两个音频之间切换。
4. 收藏、分类、搜索、排序不影响播放队列基本可用性。
5. 删除记录后原文件不删除，列表不再显示该记录。
6. 对一个音频生成字幕，字幕写回该录音。
7. 切换浅色/深色主题，主要文字和按钮仍可读。
8. GitHub Actions 构建后下载最新 IPA，确认 tag 对齐当前 commit。

